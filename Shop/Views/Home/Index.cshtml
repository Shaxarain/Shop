@model IEnumerable<NavigationProperty.Models.Player>
@{
    ViewBag.Title = "Home Page";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Магазинчик</title>
</head>
<body>
    <div>
        <h3>Проверка</h3>
        <table>
            <tr>
                <th>Продукт</th>
                <th>Картинка</th>
            </tr>
            @foreach (var p in prph)
</table>
            @*<table>
                    @foreach (var ph in ViewBag.ProductPhotos)
                    {
                        <td><p>@Html.Raw("<img style='width:80px; height:60px;' src=\"data:image/jpeg;base64," + Convert.ToBase64String(ph.ThumbNailPhoto) + "\" />")</p></td>
                    }
                    @foreach (var p in ViewBag.Productions)
                    {
                        <tr>
                            <td><p>@p.Name</p></td>
                        </tr>
                    }
                </table>*@
        </div>
    </body>
</html>